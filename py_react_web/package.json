{
  "name": "kul-hackaton2026-web3",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "server": "cd server && conda activate kulhack && python server.py",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
